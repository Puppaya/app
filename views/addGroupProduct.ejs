<%- include('header') %>
    <form method="post">
        <div class="card" style="width: 500px; margin-left: 450px; margin-top: 200px;">
            <div class="card-body">
                <div class="card-title"><h1 class="text-center">Add Category</h1></div>
                <div class="mt-4 mb-2">
                    <label for="" class="mb-2">Category</label>
                    <input type="text" name="name" class="form-control" value="<%= category.name %>">
                </div>
                <div class="mt-3 float-end">
                    <button type="submit" class="btn btn-success">Save</button>
                </div>
            </div>
        </div>
    </form>
    <%- include('footer') %>